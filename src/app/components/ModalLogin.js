

export default function ModalLogin ( {handleSubmit, handleChange} ) {

    return (
        <div className='w-full h-full fixed top-0 left-0 bg-black bg-opacity-60 flex items-center justify-center'>
        </div>
    )
}